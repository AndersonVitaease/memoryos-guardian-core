{
  "name": "memoryos-guardian-core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "typecheck:conformance": "tsc --noEmit -p tsconfig.conformance.json",
    "test": "vitest run guardianCore",
    "test:conformance": "vitest run filesystemAdapter githubAdapter vpsAdapter",
    "test:all": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
